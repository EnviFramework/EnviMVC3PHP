all:
  ## <?php $test_dir = dirname(__FILE__); ?>
  ## <?php $app_name = substr(basename(__FILE__), 0, strlen(basename(__FILE__))-9); ?>

  scenario:
    path: "<?php echo $test_dir.DIRECTORY_SEPARATOR.$app_name.DIRECTORY_SEPARATOR; ?>scenario.php"
    class_name: "Scenario"

    dirs:
      # テストファイルのディレクトリ

      common: "<?php echo $test_dir.DIRECTORY_SEPARATOR.$app_name.DIRECTORY_SEPARATOR.'libsTest'.DIRECTORY_SEPARATOR.'commonTest'.DIRECTORY_SEPARATOR; ?>"
      controller: "<?php echo $test_dir.DIRECTORY_SEPARATOR.$app_name.DIRECTORY_SEPARATOR.'libsTest'.DIRECTORY_SEPARATOR.'controllerTest'.DIRECTORY_SEPARATOR; ?>"
      constant: "<?php echo $test_dir.DIRECTORY_SEPARATOR.$app_name.DIRECTORY_SEPARATOR.'libsTest'.DIRECTORY_SEPARATOR.'constantTest'.DIRECTORY_SEPARATOR; ?>"
      models: "<?php echo $test_dir.DIRECTORY_SEPARATOR.$app_name.DIRECTORY_SEPARATOR.'libsTest'.DIRECTORY_SEPARATOR.'modelsTest'.DIRECTORY_SEPARATOR; ?>"


  app:
    url: "http://localhost/main.php"

  parameter:
    headers:
      # テスト内でデフォルト送信されるヘッダ

      referer: "http://localhost/main.php"
    get:
      # テスト内でデフォルトで付加されるクエリストリングス

      XDEBUG_PROFILE:1
    post:
      # テスト内でポストする際にデフォルトで付加されるデータ

    cookie:
      # テスト内でデフォルトで付加されるクッキー


    # デフォルトのRequestmethod
    request_method: "GET"
  extension:
    # テスト内で利用するエクステンションの設定

    DBI:
      # ここをtrueにすると、リクエストごとにロードされ、オブジェクトが生成されます
      constant: false
      class:
        resource: "<?php echo ENVI_BASE_DIR.'vendor'.DIRECTORY_SEPARATOR;?>EnviDB.php"
        class_name: "\DBInstance"
        singleton: true
      router:
        resource: "<?php echo dirname(__FILE__).'/../config/'.$app_name.'_databases.yml';?>"
