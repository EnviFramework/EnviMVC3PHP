
        $pkeys = func_get_args();
        $k = count($pkeys) - 1;
        if ($pkeys[$k] instanceof EnviDBIBase) {
              unset($pkeys[$k]);
        }
        $use_apc = %%use_apc%%;
        $res = EnviDataCache::fetchByPk(join(':', $pkeys), %%class_name%%Peer::$table_name, $use_apc);
        if ($res) {
            return $res;
        }
